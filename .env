DB_HOST=mongodb://localhost:27017/notedly

#expected to have the mongodb install
# if not run
#git clone https://github.com/cheunglai/setups.git
#cd setups && chmod +x setup.sh && ./setup.sh

#only works on linux